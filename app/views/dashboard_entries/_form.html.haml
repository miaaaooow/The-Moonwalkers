= form_for @dashboard_entry do |f|
  -if @dashboard_entry.errors.any?
    #error_explanation
      %h2= "#{pluralize(@dashboard_entry.errors.count, "error")} prohibited this dashboard_entry from being saved:"
      %ul
        - @dashboard_entry.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :content
    = f.text_field :content
  .actions
    = f.submit 'Save'
